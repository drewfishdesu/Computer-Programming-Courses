#include <iostream>
#include <cstring>
using namespace std;

int main()
{
    int size = 30;
    char word[size]

    int start;
    int endlength;
    int looplength;

    cin.get(a, size)
    cin >> start >> endlength;
    looplength = start  - 1 + endlength;
    for(int x = start - 1; x < looplength; x++)
    {
        cout << word[x]
    }

    return 0;

}