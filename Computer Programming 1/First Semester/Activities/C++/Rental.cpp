#include <iosteam>
#include <iomanip>
using namespace std;

int main()
{
    int tot_VCD;
    int num_days;
    float current_rental
    float total_rental = 0;

    cin >> tot_VCD;
    cin >> num_days;

    int i:
    for(i = 0; i < tot_VCD; i++)
    {
        cin << current_rental
        total_rental += current_rental;
    }

    total_rental *= num_days;

    cout << fixed << setpricision(2) << total_rental;

    return 0;
 }